name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.21.6

      - name: Build the Docker image
        run: docker-compose build

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        run: |
          docker tag go-backend_web ucas789/icampus:latest
          docker push ucas789/icampus:latest

      - name: Deploy to server
        env:
          SSH_PASS: ${{ secrets.SSH_PASS }}
        run: |
          sshpass -p $SSH_PASS ssh -o StrictHostKeyChecking=no lmj@widealpha.top 'docker pull ucas789/icampus:latest && docker-compose up -d'


  deploy:

    runs-on: ubuntu-latest
    needs: build

    steps:
